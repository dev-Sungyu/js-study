// task.html
// tbody 에 있는 tr들을 가져온다.
// click이벤트를 사용해서 button에 넣어준다.
// 케이스별로 구분해서 아동일경우 아동이 들어있는 tr에 background 색을 변하고 앞에 ★을 붙여준다.
// 들어온 값이 1 이상일때 .으로 들어올때 숫자가 아닐떄를 구분해준다.
const hans = "영일이삼사오육칠팔구".split("");
const inputText = document.querySelector("#inputText");
const inputBtn = document.querySelector("#inputBtn");
const output = document.querySelector("#output");
const countDiv = document.querySelector("div.number");
const footer = document.querySelector('footer');

inputBtn.addEventListener("click", () =>{
    if (inputText.value){
        
    }
})